var assert = require("assert"),
    Join = require("../Join");

describe("Join", function () {
    var join = new Join();
    //going to need the keyhashbin (ffffffffff!)

    describe("#_performJoining()", function () {
        it("should add a new subdocument with the given key to the correct item in the", function () {

        });
    });
});
